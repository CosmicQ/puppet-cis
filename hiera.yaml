---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data
hierarchy:
  - name: "Release - Major version"
    path: "os/release/major/%{facts.os.release.major}.yaml"

  - name: "OS Family"
    path: "os/family/%{facts.os.family}.yaml"

  - name: "Virtual"
    path: "virtual/%{facts.virtual}.yaml"

  - name: "Common"
    path: "common.yaml"